class Solution {
    public int[] buildArray(int[] nums) {
        int[] answer_array = new int[nums.length];
        int i=0;
        while(i<nums.length){
            answer_array[i] = nums[nums[i]];
            i++;
        }
        return answer_array;
    }
}
